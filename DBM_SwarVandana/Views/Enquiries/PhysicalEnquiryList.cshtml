@model IEnumerable<Models.Enquiries>
@{
    ViewBag.Title = "PhysicalEnquiryList";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<script>
    function AddNew() {
        window.location.href = '@Url.Action("PhysicalEnquiry", "Enquiries")';
    }
</script>
<!-- start container -->
<div class="container">
    <!-- table-info -->
    <div class="table-info">
        <!-- start table-form -->
        <div class="table-form">
            <form action="#">
                <input type="text" name="search" value="" class="input" />
                <input type="submit" value="Search" class="submit-btn" />
                <label for="From">&nbsp;</label>
                <input name="submit" type="button" value="Export" onclick="Export()" class="submit-btn" />
                <label for="From">&nbsp;</label>
                <input name="submit" type="button" onclick="AddNew()" value="Add New" class="submit-btn" />
            </form>
        </div>
        <!-- end table-form -->
        <!-- start table -->
        <div style="float: left; width: 100%">
            <table cellpadding="0" cellspacing="0" class="table">
                <!-- start row -->
                <tr bgcolor="#ccc">
                    <td class="td">Name</td>
                    <td class="td">Discipline</td>
                    <td class="td">Contact Number</td>
                    <td class="td">Date of Enquiry</td>
                    <td class="td">Source</td>
                    <td class="td">Remarks</td>
                    <td class="td">Action</td>
                </tr>
                <!-- end row -->
                @foreach (var v in Model)
                {
                    <!-- start row -->
                    <tr>
                        <td class="td">@v.Name</td>
                        <td class="td">@v.Discipline</td>
                        <td class="td">@v.ContactNumber</td>
                        <td class="td">@string.Format("{0:dd MMM yyyy}", v.DateOfEnquiry)</td>
                        <td class="td">@v.SourceId</td>
                        <td class="td">@v.RemarksByFaculty</td>
                        <td class="td">
                            <a href="@Url.Action("PhysicalEnquiry", "Enquiries", new { enquiryId = v.EnquiryId })" title="Edit"><span>Edit</span></a>
                            @*&nbsp;<a href="@Url.Action("DeleteEnquiry", "Enquiries", new { enquiryId = v.EnquiryId,type=1 })" title="Edit"><span>Delete</span></a>*@
                        </td>
                    </tr>
                    <!-- end row -->
                }
                @{if (Model.Count() == 0)
                  {
                    <tr class="row">
                        <td class="td" style="text-align: center">No record found.</td>
                    </tr>
                
                  }}
            </table>
        </div>
        <!-- end table -->
    </div>
</div>
<script type="text/javascript">
    function Export() {
        window.location.href = '@Url.Action("PhysicalEnquiryList", "Enquiries")';
    }
</script>
